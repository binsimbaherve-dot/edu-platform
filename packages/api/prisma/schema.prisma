generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model HealthCheck {
  id        Int      @id @default(autoincrement())
  message   String   @default("EduPlatform Prisma connection confirmed")
  createdAt DateTime @default(now())
}
